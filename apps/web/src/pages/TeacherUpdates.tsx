// The class blog lives on its own tab: it is the one thing on this dashboard aimed at
// families rather than at the teacher's own planning.

import ClassPhotoManager from '../components/ClassPhotoManager'

export default function TeacherUpdates() {
  return <ClassPhotoManager />
}
